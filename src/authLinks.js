
// will take a user_id and response url, build the autorization url, and send it to response url
function send() {
  console.log('offerAuthLink was called');
  return;
}

module.exports = {
  send: send
};